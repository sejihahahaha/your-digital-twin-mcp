{
  "servers": {
    "digital-twin-mcp": {
      "name": "Digital Twin MCP (local)",
      "protocol": "mcp-http/1.0",
      "type": "http",
      "healthcheck": "/api/mcp",
      "healthcheckUrl": "http://localhost:3000/api/mcp",
      "startCommand": "pnpm dev",
      "startTask": "Start Next Dev Server (pnpm dev)",
      "startCwd": "${workspaceFolder}",
      "timeout": 30000,
      "url": "http://localhost:3000/api/mcp",
      "description": "Digital Twin RAG MCP Server (local development)"
    }
  }
}


